#include <stdio.h>

int main() {
    int x = 2, n = 5;
    long long sum = 0, term = 1;

    for (int i = 0; i <= n; i++) {
        sum += term;
        term *= x;
    }

    printf("Sum of series = %lld", sum);

    return 0;
}
